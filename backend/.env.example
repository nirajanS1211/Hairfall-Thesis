# Copy to backend/.env and fill in. Read when the kernel starts (press "Restart kernel" after editing).
# TabPFN: log in at https://ux.priorlabs.ai, accept the license, copy the API key from /account
TABPFN_TOKEN=
# Optional: Hugging Face token (faster TabFM weight download)
HF_TOKEN=
