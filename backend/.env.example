# --- Gateway (main.py) ---
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/?retryWrites=true&w=majority
FRONTEND_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
CATBOOST_SERVICE_URL=http://localhost:8001
TABPFN_SERVICE_URL=http://localhost:8002
# TabFM is not deployed live (needs more RAM than free hosting provides) —
# see backend/scripts/predict_tabfm_local.py to run it on your own machine.

# --- TabPFN service (tabpfn_service.py) only ---
TABPFN_TOKEN=your-prior-labs-tabpfn-client-token-here
